[package]
name = "portty"
version = "0.2.0"
edition = "2024"

[[bin]]
name = "portty"
path = "src/main.rs"

[dependencies]
portty-ipc = { path = "../ipc" }
clap = { version = "4", features = ["derive"] }
percent-encoding = "2"
thiserror = "2"
